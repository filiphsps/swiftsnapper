namespace Snapchat {
    export class User {

    }

    export interface LoginDetails {
        username: string,
        password: string,
        google_username?: string,
        google_password?: string,
    }
}